# React + Vite
Pasos para iniciar un proyecto de React:

1️⃣ Ejecuta este comando en la terminal:
npm create vite@latest

2️⃣ Responde a las preguntas:

Project name:

mi-primer-react


Select a framework:
👉 React

Select a variant:
👉 JavaScript (o TypeScript si ya lo usas)

Seleccionar "No" en "Use rolldown-vite (experimental)"

Seleccionar "Si" en "Install with npm and start now"

Local: http://localhost:5173/
👉 Abre esa URL en el navegador
🎉 ¡Tu app React ya está funcionando!